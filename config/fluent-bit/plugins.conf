# =============================================================================
# Fluent Bit external plugins.
# The SERVICE block references plugins_file, so this file must exist. No
# external plugins are required for the Sensor. Add "Path /path/to/plugin.so"
# entries under [PLUGINS] if you need one.
# =============================================================================
[PLUGINS]
